{
  "name": "@jmdc-rehooks/use-bool",
  "version": "0.1.1",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hdpinc/rehooks.git"
  },
  "bugs": {
    "url": "https://github.com/hdpinc/rehooks/issues"
  },
  "scripts": {
    "build": "yarn run build:js && yarn run build:types",
    "build:js": "BABEL_ENV=production babel index.ts --root-mode upward -d . --extensions '.ts,.tsx' --ignore '**/*.spec.tsx,**/*.stories.tsx' -s",
    "build:types": "tsc index.ts",
    "prepare": "yarn run build",
    "test": "jest --verbose"
  },
  "peerDependencies": {
    "react": "^16.8.6"
  },
  "gitHead": "131932a0b13ea41f5e28660ba135caa70c7795a5"
}
